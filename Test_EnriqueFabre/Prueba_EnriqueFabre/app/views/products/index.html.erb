<div class="container">
<h1>Products List</h1>
<table  class="table table-bordered" style="color: white;>
 <thead class="">
  <div class="row">
    <div >
      <th>Name </th>
        <th>Color </th>
        <th>Category </th>
        <th>Size </th>
        <th>Price </th>
      </div>
    </div>
    </thead>
    <% @products.each do |product| %>
    <div class="row">
        <div >
  <tr>
    <td> <%= product.name %></td>
    <td> <%= product.color %></td>
    <td>
    <%= @category=Category.find(product.category_id)
       @category.name  %></td>
           <td> <%= product.size %></td>
           <td> <%= product.price %></td>
    <td> <%= link_to "Edit", edit_product_path(product),class: 'btn btn-primary' %>
     <%= link_to "Delete", product, method: :delete, data: { confirm: "Do you want to delete this product?"},class: 'btn btn-primary' %></td>
  </tr>

  <% end  %>
</div>
</div>
  <tr>


          <%= link_to 'NEW', new_product_path , :method => :get ,class: 'btn btn-primary'%>

    </tr>


</table>

</div>
