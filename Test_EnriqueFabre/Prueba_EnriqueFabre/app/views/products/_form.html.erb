
<div class="content">
    <strong><%= f.label "Stores" %></strong><br />
  <%= f.collection_check_boxes :store_ids, Store.all, :id, :name do |b| %>
    <div class="">
      <%= b.check_box   %>
        <strong><%= b.label   %></strong>
    </div>
  <% end %>
</div>
